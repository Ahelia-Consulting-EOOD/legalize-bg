.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.coverage
catalog.db
.superpowers/
# Refresh runner artifacts (derived, per-run)
.refresh-state.json
refresh-run.log
gate-report.json

# Playwright MCP browser session artifacts (snapshots/console logs)
.playwright-mcp/

# Cloudflare export artifacts (export_cf output — never commit)
cf-export/

# FR-034 sweep verification baseline (derived from catalog.db, per-run)
.fr034-baseline.json

# D-058 (iv) per-(law_id, article) baseline (derived from catalog.db,
# per-run)
.article-baseline.json

# D-058 (iv) full article-check finding list (derived, per-run — the
# adjudication input, rewritten by every failing run)
.article-check-findings.txt

# FR-034 sweep + rebuild run logs (census evidence, per-run — keep the
# files on disk, never commit them)
refresh-fr034*.log
rebuild-fr034*.log

# Raw ДВ material HTML, cached by fetcher/dv (acquisition artefact, local only;
# see data/dv/README.md). Never committed.
data/dv/cache/
