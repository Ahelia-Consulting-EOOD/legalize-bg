# Decision Log

| ID | Date | Decision | Rationale | Status |
|----|------|----------|-----------|--------|
| D-001 | 2026-04-19 | Contribute to Legalize upstream (not fork) | Get ~70% infrastructure free (transformer, committer, git ops, CI/CD, CLI); Bulgaria joins 25-country ecosystem | Active |
| D-002 | 2026-04-19 | lex.bg = bootstrap only, DV = ongoing | Avoid ongoing scraping dependency; DV is the authoritative gazette source for forward tracking | Active |
| D-003 | 2026-04-19 | lex.bg = validation oracle | Compare consolidation engine output against lex.bg consolidated texts to measure accuracy | Active |
| D-004 | 2026-04-19 | Markdown + YAML format (not Akoma Ntoso) | Legalize ecosystem compatibility; entire ecosystem uses Markdown with YAML frontmatter | Active |
| D-005 | 2026-04-19 | Git + SQLite hybrid storage | Git for versioned text (each amendment = commit with GIT_AUTHOR_DATE); SQLite for temporal queries and indexing | Active |
| D-006 | 2026-04-19 | Municipal = Phase 6 | After national pipeline is stable; 265 municipalities each with different website structure | Active |
| D-007 | 2026-04-19 | Host at Ahelia-Consulting-EOOD | Private repo for MCP server, corruption research tooling, municipal pipeline; later upstream fetcher/bg/ to Legalize | Active |
| D-008 | 2026-04-20 | Repo type = data_pipeline_repo | Per Ahelia documentation standard classification | Active |
| D-009 | 2026-04-20 | Full governance pack adopted | OWNER-DIRECTIVES, COVERAGE-FLOOR, IMPLEMENTATION-PREFLIGHT governance documents | Active |
| D-010 | 2026-04-20 | Shared `RateLimitedSession` across all transports | Separate rate-limit timers per transport could stack into >1 req/sec against lex.bg even while each transport "respects" its own limit. Single session = single timer = contract-compliant rate limit whether the pipeline is fetching doc pages, tree pages, or both in interleaved order. | Active |
| D-011 | 2026-04-20 | `CloudflareChallenge` raised, not retried | Contract line 154 says stop immediately on CF challenges — retrying a challenge signals bot-like behavior and escalates the block. The exception bubbles out of the bootstrap so the operator triages before re-running. | Active |
| D-012 | 2026-04-20 | ELI uses ASCII-transliterated slug, not Cyrillic | Cyrillic is legal in IRIs but percent-encodes to unreadable noise (`%D0%B7%D0%B0%D0%BA%D0%BE%D0%BD`…). ASCII slug also keeps ELI and filesystem filename in lockstep, sharing one `generate_slug` function. `rango` stays Cyrillic per data-model.md convention. | Active |
| D-013 | 2026-04-20 | Cross-category doc_id dedup in crawler (first-wins) | `Конституция` appears on every tree page as a sidebar link (104×) and a few acts legitimately appear in two related categories. Dedup in `crawl_all` (not the bootstrap loop) — the crawler's contract is to return canonical acts, not lex.bg UI artifacts. Iteration order (laws → code → ords → regs → reg_laws) places Конституция in `laws` (correct) and the правилник for radioactive waste in `regs` (correct). | Active |
| D-014 | 2026-04-20 | Full bootstrap delivered on `bootstrap/phase-1a` branch with periodic push | Run produces ~3,573 commits over ~2 hours. Running directly on `main` means an aborted run leaves a half-scraped main to clean up; landing on a feature branch keeps `main` clean until the run is reviewed, and `--push-every 250` gives incremental remote backup without the pathological cost of pushing per commit. 3× retry on push failure absorbs transient network blips. | Active |
| D-015 | 2026-04-20 | Bulgarian-month-name DV dates supported in metadata parser | Real `.HistoryOfDocument` text uses `"16 Февруари 2016г."`, not the numeric `13.02.2016 г.` form. Without this, every amendment date silently parses to `None`. Regex alternation accepts both forms with case-insensitive matching on the Bulgarian month names. | Active |
| D-016 | 2026-04-20 | `GIT_AUTHOR_DATE` formatted as ISO 8601 with time + timezone | Git rejects bare `YYYY-MM-DD` with "fatal: invalid date format" and exits 128 — discovered during the first Task 11 run, which silently bundled 3,573 files into 121 wrong-dated commits because the error was caught by the per-act try/except. `_format_author_date()` now emits `YYYY-MM-DDT00:00:00+00:00` and sets both `GIT_AUTHOR_DATE` and `GIT_COMMITTER_DATE` so `git log --before` reconstructs legislative history chronologically. | Active |
| D-017 | 2026-04-20 | Pre-1970 publication dates clamped to `1970-01-01` for `GIT_AUTHOR_DATE` | This git build refuses negative Unix timestamps in every input format (ISO 8601, RFC 2822, internal `@timestamp`). Many Bulgarian acts predate the epoch (1947 Amnesty Act, 1950s commercial codes). True publication date is preserved in the `Source-Date` body line; chronology of pre-1970 acts collapses to epoch but still orders them before any 1970+ act, which is the property that matters for `git log --before` queries. | Active |
| D-018 | 2026-04-20 | Bootstrap recovery via local replay, not re-crawl | When the date-format bug was found mid-run, the Markdown files on disk were already correct (the parsing pipeline worked; only `git commit` failed). `scripts/rebuild_bootstrap_commits.py` reads YAML frontmatter from each `.md`, resets the branch, and replays commits locally — 2 min 20 s for 3,573 commits vs ~2 hours for a clean re-fetch. Saved ~1h40min of lex.bg traffic and preserved the "one real lex.bg pass per bootstrap" discipline (bootstrap is meant to happen once, ever). | Active |
| D-019 | 2026-04-20 | `bootstrap/phase-1a` landed via `git push --force-with-lease` | Destructive push was acceptable because: (a) the branch is a feature branch, not `main`; (b) the force-with-lease safety check protects against clobbering unseen remote commits; (c) the broken pre-recovery state was tagged locally (`bootstrap-phase-1a-broken-backup`) for recovery if something went wrong during the replay. | Active |
| D-020 | 2026-05-09 | Phase 1b ships through MCP server, not direct file access | All three target clients (Claude Code, Claude Desktop, OpenAI Codex) support MCP natively. Per-query token economics: a typical legal-research session becomes 10-20 cheap tool calls (~500-2k tokens each) instead of 5-10 enormous file reads (10-50k tokens each). FTS5 ranking, article-level retrieval, date-qualified queries, and §7 data-quality semantics all become server-enforced contracts instead of per-conversation reinventions. Direct file access remains a fallback (MCP doesn't replace it) — the question was only whether to add a fast/precise path on top, and the answer is yes. Full pros/cons in `docs/plans/2026-05-09-phase1b-mcp-design.md` §1. | Active |
| D-021 | 2026-05-09 | FastMCP for the Python MCP SDK | Decorator-based API generates JSON-RPC plumbing and `tools/list` descriptions automatically from type hints + docstrings. Tool-description quality directly drives Claude Code's ability to pick the right tool — and FastMCP's docstring-to-description flow makes keeping descriptions in sync with behavior trivial. Built on top of the official `mcp` SDK (transitive dep), so escaping to low-level for any single tool that demands it remains possible without parallel-universe code. | Active |
| D-022 | 2026-05-09 | Bulgarian search via FTS5 + symmetric `bg_normalize` pre-normalization | SQLite FTS5's `unicode61` tokenizer handles Cyrillic lowercasing and diacritic folding. Bulgarian morphology (definite-article suffixes `-та`, `-то`, `-те`, `-ят`, `-ът`, `-ите`, `-ето`) is handled by pre-normalizing both indexed text and queries through the same `bg_normalize()` function — symmetric, no custom SQLite C tokenizer, no extension loading, pure Python. Covers ~70-80% of grammatical-form friction. Snowball-style Bulgarian stemmer + legal-term synonym dictionary deferred to Phase 1b.3, decision driven by real query patterns observed via 1b.3 logging. | Active |
| D-023 | 2026-05-09 | `provisions` table populated to alinea level from day one (Phase 1b.1) | Per "richer functionality from day one, do not cut corners". Index build emits one row per `**Чл. N.**` anchor (`paragraph=NULL`, full-article text) AND one row per `(K)` alinea (`paragraph=K`, alinea text). `text` column stored alongside `text_hash` so `get_article` is a SQL lookup with no runtime Markdown parsing. Phase 2 (temporal index, FR-001) and Phase 4 (consolidation engine) inherit a fully-utilized schema with no migration mid-roadmap. Cost: ~5x more rows in `provisions`, build time <1 min → ~3-5 min — both fine. | Active |
| D-024 | 2026-05-09 | `get_law` returns a structured typed-dict, not a Markdown string | The model gets metadata fields (titulo, identificador, fecha_publicacion, ultima_actualizacion, dv_issue, dv_year, effective_date, eli, amendment_history, commit_hash) alongside `body_markdown`. Bare-string was the corner-cut: it would have forced the model to re-parse frontmatter every call OR throw away citation metadata. FastMCP renders typed-dicts naturally. Container-view §7 tool signature updated from `-> str` to `-> GetLawResponse`. | Active |
| D-025 | 2026-05-09 | `index/migrations.py` exists from Phase 1b.1, not deferred | Forward-only schema versioning from day one. Even though the SQLite catalog is fully derivable from git (drop-and-rebuild always works), establishing the migrations pattern early benefits Phase 2 (law_versions backfill from amendment_history), Phase 4 (amendment events), and any downstream caller who builds annotations on top. The 1b.1 migration set is small (initial schema + provisions.text column + laws_fts virtual table) but locks the practice. | Active |
| D-026 | 2026-05-09 | Error taxonomy returns structured `{code, payload}` to model | Tool errors are first-class outputs, not opaque MCP transport failures. Eight codes: `LAW_NOT_FOUND`, `AMBIGUOUS_NAME`, `NO_VERSION_AT_DATE`, `DATE_UNCERTAIN` (warning, not blocker), `INVALID_ARTICLE_SPEC`, `ARTICLE_NOT_FOUND`, `INDEX_STALE`, `INDEX_MISSING`. Each carries a structured payload the model can act on (suggestions, candidates, available_articles, etc.). FastMCP serializes `ToolError(code, payload)` exceptions into the MCP response envelope. This is the surface that makes §7 data-quality cases server-enforced contracts instead of per-conversation reinventions. | Active |
| D-027 | 2026-05-09 | Phase 1b is a three-mandatory-milestone plan (1b.1 / 1b.2 / 1b.3) | Per user direction "polished final perfect versions, with a properly documented roadmap and phases to reach it, starting with the B". 1b.1 ships all three tools with workflows A+B end-to-end; 1b.2 hardens JSON schemas + error taxonomy + perf-budget enforcement for workflow C (structured backend); 1b.3 packages, documents deployment, adds observability, and (data-driven) layers in Bulgarian stemmer + legal synonyms. None of the three is optional. Phase 2+ (`history`, `diff`, `amendments_in_period`) is strictly out of Phase 1b scope but 1b.1 schema is built to enable it without migration. | Active |
| D-028 | 2026-05-09 | Phase 1b.2 closure design choices | (a) FR-016 fixed via stop-word reject (5 Bulgarian rang words) raising new `QUERY_TOO_BROAD` rather than BM25 LIMIT pushdown — smaller code, eliminates cold-call case entirely, fits the existing `ToolError` pattern; additive per Surface 3. (b) `tools.json` source of truth = `mcp_server/export_tools.py`; the committed file is an artifact, not hand-edited; `--check` mode prevents drift. (c) Error-taxonomy double publication (md + json + parity tests against runtime registry). (d) Cold-call test added as a separate file (`tests/perf/test_cold_calls.py`) with relaxed 250 ms search budget; shared `tests/perf/conftest.py` warmer keeps OS cache hot so budgets measure SQLite/FTS5 not disk I/O. (e) Idempotency contract stays as documentation, not runtime check (tools are read-only by construction). Closes deferrals D-2026-05-09-03 and D-2026-05-09-06; the four FR-013/14/15/17 deferrals remain Open per their 1b.3/Phase-4 targets. Plan: `docs/plans/2026-05-09-phase1b2-hardening.md`. | Active |
| D-029 | 2026-05-09 | Phase 1b.3 closure design choices | (a) FR-013 closed conservatively: only the 3-char `ият` suffix is added to `_BG_DEFINITE_SUFFIXES` (with MIN_STEM=3) — addresses the canonical `новият/нов` asymmetry the FR text locks. Other long-form suffixes (`ите`, `ия`, `ето`) deliberately NOT added because they would mangle plural-noun endings or break existing index/query symmetry (the original 1b.1 comment block in `index/fts.py` documents the asymmetries that WOULD have appeared). Multi-syllable consonant-stem cases like `българският/български` need a proper Bulgarian Snowball stemmer and stay out of 1b.3 per the FR-013 text. (b) FR-015 implemented as TWO orthogonal layers: a hand-curated abbreviation dictionary (`index/synonyms.py`, 22 entries) that rewrites single-token queries pre-FTS5, AND a stable rang-aware tier sort in `search_fts` (laws/codes tier 0; implementing/regulations/ordinances tier 1) so parent laws outrank their implementing regs even when bm25 prefers the latter. The dictionary is one-direction (abbrev → canonical) because the long form already reaches the act via FTS5 title matching. (c) FR-017 implemented as OPT-IN body-snippet via a new `include_body=False` parameter on `search`. Reason: empirical measurement on the live catalog showed even TOP_N=2 body fetches blow the 100 ms warm-search budget (254 ms p95) because the largest indexed bodies are 1+ MB and SQLite materializes the full row regardless of `substr()`. Opt-in design preserves the 100 ms / 250 ms warm/cold budgets for default callers; callers that need body context pass `include_body=True` and accept the ~150 ms overhead. The new `include_body` parameter is additive per Surface 3 (default False preserves prior signatures). (d) `body_snippet` on `SearchHit` is non-optional (`str`, possibly empty) rather than `Optional[str]` so caller code stays branchless. Closes deferrals D-2026-05-09-01, -02, -04; the only remaining open deferral is D-2026-05-09-05 (FR-014, Phase 4 incremental rebuild). Plan: `docs/plans/2026-05-09-phase1b3-polish.md`. | Active |
| D-030 | 2026-06-21 | Corpus wholesale re-scrape ("re-photograph lex.bg") delivered on `refresh/2026-06` | Parallel track to Phase 2. Re-fetched all current lex.bg acts and refreshed the corpus as a fresh snapshot — lex.bg performs the legal consolidation, so re-scraping yields 100%-accurate current text (D-003). New top-level `refresh.py` orchestrator reuses the Phase-1a fetcher/assembler/commit machinery and touches no protected surface. Outcome: 26 `[nova]` + 184 `[reforma]` + 66 `[popravka]` = 276 single-file corpus commits; 3,305 acts effectively unchanged; 18 acts gone from the tree KEPT (repealed/superseded — `estado` untouched, report-only per owner directive); 0 fetch errors; 0 Cloudflare. Key engineering decisions: (a) EXISTING acts reuse their on-disk slug (`path.stem` == the MCP handle) and never regenerate it; only ADDED acts mint new slugs deduped against all existing corpus slugs. (b) A staleness probe crawls one page past each hardcoded `CATEGORIES_CONFIG` count to catch acts on tree pages added since April WITHOUT modifying the protected discovery surface; this run reported `STALE={}` (the hardcoded counts are still complete). (c) `_git_commit_typed` is a no-op when nothing is staged — this gives resume idempotency AND absorbed 16 benign `classify_change` over-triggers (acts flagged changed whose re-assembled bytes were byte-identical), so zero empty commits were made. (d) ADDED acts translate the lex.bg tree slug to the corpus directory name via `CATEGORY_DIRS` (mirroring bootstrap) — a wrong-directory bug caught by code review and fixed (TDD) before merge. Change detection is sound because the fetcher (text_parser/metadata/assembler) is frozen at the April bootstrap, so an unchanged act re-assembles byte-identical. SQLite index rebuilt (3,599 acts). Plan/spec + close-out: `docs/sync/HANDOFFS/2026-06-21-corpus-rescrape-refresh.md`. | Active |
| D-031 | 2026-06-21 | Phase 2 honest single-version temporal semantics | history()/amendments_in_period() backed by amendments table from amendment_history (full value now); diff()/get_law(date) return correct-but-limited results until a write-side accumulates more text versions — chose this over fabricating law_versions rows pointing historical valid_from at the bootstrap commit, which would make get_law(date) return current text and falsely claim it was in force on a past date. **Superseded by D-042 (2026-06-21): FR-020 now populates real multi-version law_versions from git history, so diff()/get_law(date) return genuine historical results.** | Superseded (D-042) |
| D-032 | 2026-06-21 | Defer the Bulgarian Snowball stemmer out of batch 2.x-a (→ FR-021) | The canonical open recall gap (`българският` vs indefinite `български`) can't be closed by extending the definite-article suffix table — it needs stripping bare `-и`, which mangles plural nouns (`закони`, `поръчки`) and collides with the already-REJECTED `ия`/`ите` suffixes (`решения → реше`); a genuine POS-aware morphology problem. A real stemmer (a) conflicts with D-022 (pure-Python, no external NLP libs — superseding it is owner-ratifiable, not a quiet default), (b) must be applied symmetrically across the whole corpus → forces a full re-index and risks the locked ranking behavior (two-tier title match, FR-015 synonym + rang-tier sort, FR-013 `новият→нов`), and (c) needs a full-corpus precision/recall eval harness before it can be trusted. Shipping a half-measure (`-и` stripping) would regress precision for narrow recall gains. Scoped as FR-021, its own eval-gated effort; no `bg_normalize` change in 2.x-a. | Active |
| D-033 | 2026-06-21 | FR-018 article ranges: add a `get_articles` tool; `get_article` rejects ranges | Chose a new additive tool over (a) a polymorphic `get_article` return (dict-or-list = ambiguous output schema) or "always list" (breaking change to every single-article caller). `get_articles(law, articles, date)` returns `{law_id, articles:[{article, paragraph, text, text_hash}], commit_hash, warnings}` — one shared commit/warnings channel (all entries share law+date → same version). Range expands via `_legal_article_sort_key` over `[start, end]` inclusive (incl. suffixed `14а`), gaps skipped; empty range → ARTICLE_NOT_FOUND. `get_article` now raises INVALID_ARTICLE_SPEC (existing code) with a `get_articles` hint on a range instead of silently returning only the first article (the FR-018 bug) — signature + success shape unchanged. Additive per Surface 3 (`tools.json` 1.1.0→1.2.0); no new error codes. Preflight: `docs/process/IMPLEMENTATION-PREFLIGHT-2026-06-21-fr018.md`. | Active |
| D-034 | 2026-06-21 | FR-011 degenerate-act disposition: waive-with-explanation, no invented data | Re-count vs live 3,599 corpus unchanged (7 empty-titulo ⊂ 121 null-fecha_publicacion). All 121 nulls are faithful to the lex.bg source (D-003 oracle; corpus is the photograph, frozen parser produced null twice — April bootstrap + June re-scrape): 104 municipal council acts, 2 judicial ethics codes, 7 §7.3 numeric phantoms (already handled by `<doc_id=N>`), 8 residual professional-body/court bylaws (no date signal anywhere). Chose a WAIVERS registry (`docs/data/fr-011-degenerate-triage.md`) over backfilling — fabricating fecha_publicacion to satisfy G2 would corrupt data to pass the gate. G2 treats these as known waivers. No corpus/schema change. The 8 residuals (esp. national-body acts) flagged as optional future source-verification → if lex.bg shows a ДВ date the parser missed, that's a parser-coverage FR, not hand-entry. | Active |
| D-042 | 2026-06-21 | FR-020 time-machine: multi-version `law_versions` from git history (supersedes D-031 single-version semantics) | `index/build.py` now writes one `law_versions` row per git commit of each act's file: `valid_from` = commit author-date (the legislative/DV date per the commit-date discipline), `valid_to` = day before the next version's `valid_from` (INCLUSIVE; NULL for the latest), `commit_hash` = that commit — except the LATEST row carries HEAD (so `get_law`'s working-tree fast path stays valid: `head:path` == the working tree). Same-author-date commits collapse to that day's last commit (distinct `valid_from`, no inverted ranges). **Perf:** the full build precomputes the whole-corpus version map in ONE `git log --name-only` pass (`_all_file_versions`), NOT one `git log` per act — a naive per-act approach measured ~20 min (with `--follow`) / ~8 min (without); the single-pass is **~34 s, faster than the pre-FR-020 ~45 s baseline**. No `--follow` (stable slugs → no renames; ordering handled by a sort). The FR-014 incremental path uses a per-act `git log` for its few changed acts. The Phase-2 query layer was already multi-version-ready (`version_at_date` predicate; `get_law`/`diff` branch to `git show`/`git diff`), so the time-travel tools light up with no query change: `diff()` returns a real git diff across versions, `get_law(name, date)` returns the historical text, and `version_at_date` resolves the in-force commit per date. Falls back to a single row when an act has no git history (uncommitted test corpora). No SQLite schema change; `bg_normalize` untouched. Also scopes the FR-014 incremental pointer-bump to the current version (`WHERE valid_to IS NULL`) — fixing the D-040/D-041 hazard now that multiple rows exist. Verified by a real-git oracle test (2 dated commits → 2 versions, boundary-correct resolution, real diff) + live build on the 3,599-act corpus. Preflight: `IMPLEMENTATION-PREFLIGHT-2026-06-21-fr020.md`. | Active |
| D-041 | 2026-06-21 | FR-014 incremental index rebuild via git-diff against the indexed commit (closes deferral D-2026-05-09-05) | `index/build.py` gains `build(..., incremental=True)` / `--incremental`. Chose **git diff** of `laws.current_commit`→HEAD over a per-act content-hash column (no SQLite schema change — Surface 4 untouched; the corpus is already git-tracked so the diff is authoritative). Re-indexes only Added/Modified acts (DELETE then INSERT per `law_id` — keeps FTS5 ids correct), removes Deleted acts, and bumps unchanged acts' commit pointers to HEAD (`UPDATE laws SET current_commit`, `UPDATE law_versions SET commit_hash=HEAD WHERE commit_hash=base` — an unchanged act's file is byte-identical at HEAD). Falls back to a full rebuild when the catalog is empty/inconsistent or the base commit isn't in history; atomic (single commit → parse error rolls back). Per-act indexing extracted to the shared `_reindex_act` helper (DRY; full-build behavior byte-identical). Verified by an **oracle test** (incremental == full rebuild of the same HEAD, byte-identical content tables) + a live smoke on the 3,599-act catalog. Default stays full rebuild. **FR-020 interaction:** the `WHERE commit_hash=base` pointer-bump assumes one law_versions row per act; revisit when multi-version rows land. Preflight: `IMPLEMENTATION-PREFLIGHT-2026-06-21-fr014.md`. | Active |
| D-040 | 2026-06-21 | FR-023 closed by serializing tool DB access behind a lock (not per-connection pooling) | The shared single sqlite3 connection across FastMCP worker threads was unsafe for concurrent execute (InterfaceError race; and the removed FR-019 UDF deadlocked it — D-036). Chose a `threading.Lock` serializing all tool DB access in `build_app` over per-thread/per-call connection pooling because: (a) the catalog is READ-ONLY at runtime, so serializing reads is correct and SQLite already serializes internally; (b) per-call latency budgets are unchanged (the lock is uncontended single-threaded); (c) per-connection pooling conflicts with the test design (shared in-memory conn + direct mutation) and would be a larger refactor. Applied via a `functools.wraps` registration helper so FastMCP's tool-name/input-schema inference is preserved (tools.json parity green). Trade-off: coarse lock held across a tool's file/git I/O too (e.g. `diff`), so a slow historical call briefly serializes others — acceptable for the expected few-agent load; true-parallel per-connection reads remain a future optimization if throughput demands it. Concurrency regression test added. | Active |
| D-039 | 2026-06-21 | Legal posture: we BUILD OUR OWN structure from the free legal TEXTS — we reuse ZERO of any source database (owner) | Governs and clarifies the APIS/lex.bg sourcing notes (D-037/D-038). The project does NOT copy, extract, or re-utilize any provider's DATABASE — not its compilation, selection/arrangement, structure, metadata schema, identifiers, or presentation. It independently fetches the public-domain legislative TEXTS (нормативни актове — ЗАПСП чл. 4, expressly NOT objects of copyright) and transforms them into its OWN original structure built on the Legalize project: Markdown+YAML frontmatter, the project's own SQLite schema, its own slugs/identifiers and organization. The sui generis database right (ЗАПСП чл. 93б/93в) protects a maker's investment in compiling/structuring THEIR database; fetching the underlying free texts and building an independent work does not re-utilize that database, and the provider ToS clauses ("копиране на базите") target reuse of their compilation — which the project does not do. This is the standing posture for BOTH the lex.bg national bootstrap (D-038/FR-024) and the APIS municipal bootstrap (D-037/FR-022); migrating to authoritative primary sources (ДВ/official) further strengthens it. | Active |
| D-038 | 2026-06-21 | National corpus to be re-sourced from authoritative primary sources; lex.bg bootstrap provenance to be retired (→ FR-024) | The national corpus was bootstrapped from lex.bg (D-002/D-003). Like APIS for the municipal corpus (D-037 / plan §8), lex.bg asserts a чл. 93б database right + a reference-only use restriction (verified: "Базите данни в справочника на Lex.bg са обект на авторско право … предоставени за свободно ползуване само за справочни нужди. Копиране на базите без съгласието на Lex.bg представлява правонарушение!"). The legislative TEXTS are public-domain (ЗАПСП чл. 4) and the corpus is our OWN independent structure reusing zero of lex.bg's database (D-039); the remaining item is to retire the lex.bg *bootstrap provenance* in favour of authoritative primary sources — an authoritativeness upgrade as much as a legal-posture one. Remediation (FR-024): migrate the corpus's authoritative sourcing to legally-clean primary sources — Държавен вестник (promulgation/amendments — already the intended ongoing source per D-002) + official consolidations — and carefully retire the lex.bg-derived layer; public documentation references the authoritative sources, not the bootstrap method. This raises authoritativeness, not just legal posture. Sequenced after the in-flight MCP work; planned, not executed. | Active |
| D-037 | 2026-06-21 | FR-022 municipal sourcing = APIS-as-bootstrap (one-time) + per-municipality official-site adapters for ongoing (owner) | Owner directive: treat APIS `obshtini.bg` exactly like lex.bg for the national corpus — a ONE-TIME bootstrap snapshot (the D-002/D-003 pattern: bootstrap/oracle, not an ongoing source) — and IN PARALLEL build/identify catalog + access adapters for each municipality's OWN website (the legally-mandated publication channel per ЗМСМА чл. 22(2)) as the durable ongoing source. This keeps the APIS dependency transient (like lex.bg) while the municipal sites become authoritative. Legal posture (D-039): we fetch only the public-domain TEXTS (ЗАПСП чл. 4) and build our OWN structure (Legalize format + own schema/slugs), reusing ZERO of APIS's database — so the чл. 93б compilation right is not re-used and the ToS "копиране на базите" clauses (which target DB reuse) don't bite; the bootstrap pulls free texts only, kept minimal, and the parallel municipal-site adapters are the durable source. Plan: `docs/plans/2026-06-21-fr022-municipal-corpus.md`. Execution left for a future session (gated on PR #3 merge + MCP-track completion). | Active |
| D-036 | 2026-06-21 | FR-019 resolves Cyrillic titles in Python, not via a SQLite UDF (review C1) | The 2.x-a comprehensive code review found the FR-019 `pylower` Python-callback UDF **deadlocks** the live server under FastMCP's worker-thread pool on the shared `check_same_thread=False` connection — a GIL ↔ connection-mutex lock-order inversion (reproduced: 20 concurrent title lookups wedged, faulthandler-confirmed; invisible to the single-threaded suite). Fix: drop the UDF and fold case in Python (`str.casefold`) over the candidate rows in `resolve_name_to_law_id` step 3 — no in-SQL callback, no schema change, no re-index; behavior preserved (FR-019 tests green) + a bare-connection guard test added. Also fixed review M1 (reversed range `чл. 16-14` → INVALID_ARTICLE_SPEC with hint, not a misleading ARTICLE_NOT_FOUND) and M2/M3 (docstring + reversed-range/NO_VERSION_AT_DATE coverage). The deeper shared-single-connection-across-threads model (which also yields a rare InterfaceError race, pre-existing since Phase 1b) is tracked as **FR-023** for 2.x-c. | Active |
| D-035 | 2026-06-21 | Municipal legislation corpus prioritized ASAP (supersedes D-006 Phase-6 timing) → FR-022 | Owner directive 2026-06-21: build a proper municipal corpus as a SEPARATE track, prioritized ASAP, rather than waiting for Phase 6 (D-006). Trigger: FR-011 found 104 municipal council acts present only as lex.bg local entries without ДВ dates; municipal acts DO appear in Държавен вестник, so the correct fix is dedicated municipal coverage with authoritative metadata, not a permanent waiver. This decision re-times D-006 (does not negate its substance — municipal is still a distinct corpus with per-municipality site variance across 265 municipalities). Scope/plan to be brainstormed before any code (FR-022); the `municipal/` directory is already a reserved protected surface. NOT part of batch 2.x-a — recorded + prioritized only. | Active |
| D-047 | 2026-06-29 | **P0 corpus-integrity finding + remediation decisions:** the lex.bg body parser dropped 3 subdivision classes → corpus-wide loss of ДР definitions + ПЗР bodies across ALL 3,599 acts | **Finding (evidence: `docs/research/2026-06-29-parser-data-loss-forensics/`).** `fetcher/bg/text_parser.py` `CLASS_MAP` is a CSS-class **allowlist** (`find_all(class_=keys)` → unmapped = silently dropped). It omits `AdditionalEdicts` (Допълнителни разпоредби heading), `FinalEdictsArticle` (§ definition/transitional bodies), and `FinalEdicts` (Заключителни разпоредби КЪМ… heading). Measured over the full corpus: **7/3599 acts retain a ДР heading (0.19%), 5/3599 retain any real § provision (0.14%)** — ~100% loss of all legal definitions and all §-numbered transitional/final provisions across every category. A separate heading-concatenation defect affects ~30% of acts. `catalog.db` (3,599 laws / 3,851 versions) and FR-020 versions are built on the corrupted text → contaminated. Original trigger: `FINDING-zuo-consolidation-incomplete.md` (ЗУО). Root cause = parser built from an incomplete class enumeration; tests (`test_text_parser.py:58`) encoded the same blind spot. **Recoverable** (parser bug, not data corruption). **Resolved decisions (owner 2026-06-29):** (D1) re-bootstrap the FULL national corpus with the fixed parser, oracle-gated — the whole 3,599-act bootstrap already shipped, so NO "caught early" reprieve; (D2) fix the heading-concatenation defect in the same pass; (D3) **harden against the defect class, not the instance** — invert the parser to **keep-by-default** (chrome denylist) + add a **class-AGNOSTIC coverage gate** asserting ~0 uncovered legal text per act over 100% of acts (completeness proven by measuring uncovered text, NOT enumerating classes — `COMPLETENESS.md`); (D4) model the re-bootstrap as an FR-020 **corrective baseline**, not a `[reforma]`, so version timelines aren't polluted. **New obstacle (2026-06-29):** live lex.bg now returns a **Cloudflare 403 challenge** (client detects+stops per D-011) → re-sourcing 3,599 acts must solve CF or pull from authoritative primary sources (ties to D-038/FR-024: ДВ + official consolidations). **Interim:** ЗУО (ldoc 2135802037) re-scraped 2026-06-29 via the fixed-parse path from the 2026-06-21 capture (unchanged since dv 81/2024) — `laws/zakon-za-upravlenie-na-otpadatsite.md` now complete (+66,420 chars; ДР §1 точки + ПЗР bodies restored; 173 articles preserved) — **oracle-verified COMPLETE**: 276/276 source legal subdivisions present, §1 definitions block present verbatim, 25 ДР/ПЗР headings all with bodies, 8 приложения (`ZUO-VERIFICATION.md`). Protected surface (`fetcher/bg/`) → parser fix requires IMPLEMENTATION-PREFLIGHT; full remediation plan (steps 1-5) in `docs/plans/2026-06-29-parser-remediation-plan.md`. **2026-06-29 update:** 12 consumer-priority acts (ТЗ, ЗПП, ЗООС, ЗНА, ЗЗП, Закон за храните, ЗВСН, ЗДДС, ЗКПО, Закон за счетоводството, ЗМДТ, ЗАДС) re-scraped **fresh from lex.bg** — Cloudflare cleared via a real-browser `cf_clearance` cookie reused in the rate-limited `requests` session (**path A proven viable**) — and re-parsed with the fixed parser; **all 12 strict-coverage COMPLETE** (0 missing source subdivisions). ЗАДС has NO base Допълнителни разпоредби (definitions in `Чл. 4`) → **owner directive (2026-06-29): the strict source-vs-output coverage check is the SOLE acceptance gate applied to EVERY act; base-ДР/§1 structure heuristics are rejected as false-negative-prone.** **REMEDIATED 2026-07-01 (merge `25a79389`).** Full corrective re-bootstrap complete: ~3,180 non-law acts re-scraped (3142 `[popravka]` + 12 `[reforma]` + 1 `[nova]`) via the CF-hardened path (Task 9 path A — `cf_clearance` cookie re-minted through Playwright on each ~30-min TTL expiry; a mid-run DNS outage failed 48 acts + 2 connection-resets, all recovered to **0 errors** via a state-clear re-run). Coverage gate ran as the sole per-act check: **7 gate-fails, all vision-confirmed content-less lex.bg stub pages** (pre-existing FR-011 degenerates, 322-byte frontmatter-only) → correctly skipped, no legal content lost. **Допълнителни разпоредби restored across 1,826 acts** (was 31 on main). **D4 handled (Task 13):** `[popravka]` is a corrigendum, not a legal amendment → excluded from FR-020 version-boundary derivation (`index/build.py:_all_file_versions`/`_git_file_versions`), collapsing spurious multi-version acts **2220→225** (real amendments only); `get_law(АПК, historical_date)` now returns the corrected text (oracle tests in `tests/index/test_time_machine.py`). **Task 0** deploy-guard added (`LEGALIZE_CORPUS_DEFECTIVE` refuse-to-serve). `catalog.db` rebuilt (3,600 acts, `verify_catalog` OK); suite 441 pass (2 perf-budget tests load-flaky, non-regression). DRS trigger: tag `corpus-d047-remediated` + `docs/sync/CORPUS-STATUS.json`. **Residual:** FR-020 historical versions for the ~12 `[reforma]` acts still carry pre-fix text on their OLDER (non-latest) version rows (inherent to git-commit-based versioning; latest = corrected). | Remediated |
| D-048 | 2026-07-01 | **Stop backdating `GIT_COMMITTER_DATE` on corpus commits** — set only `GIT_AUTHOR_DATE` to the legislative date; committer-date stays at real commit time. Amends D-016. | Owner decision (2026-07-01). During the D-047 re-bootstrap, `refresh._git_commit_typed` set BOTH author and committer date to each act's legislative date (D-016). Pushed to shared `origin/main`, this made the branch tip appear to move **backwards** (newest commit dated 2026-06-05 < prior tip 2026-07-01), tripping a downstream (DRS) session's reset/force-push alarm, and defeated date-based freshness monitoring — a body-completeness fix changes no legislative date, so a consumer keying off commit-date cannot see the refetch. **Verified no consumer relies on committer-date:** FR-020 version derivation reads **author-date** (`git log --format=%ad`); `get_law(date)`/`diff()` resolve via the `law_versions` table; the `git log --before` "legislative reconstruction" was a documented-but-unused convenience, superseded by the FR-020 time-machine. Fix leaves committer-date at real time → `git log` orders correctly and freshness is monitorable. **Applies going forward** (bulk re-bootstrap + future); the already-pushed laws branch + 6 priority acts keep their backdated committer-dates (no history rewrite on shared refs). **Downstream note:** detect corpus freshness by CONTENT (e.g. `grep -c "Допълнителни разпоредби"`) or `ultima_actualizacion`, NOT commit author-date (legislative by design). | Active |
| D-049 | 2026-07-01 | **P1 finding: the corpus omits whole lex.bg act-type categories** (inter-act analogue of D-047). The bootstrap (D-002) crawled only 5 tree categories (`laws`/`code`/`ords`/`regs`/`reg_laws`) and silently dropped every other normative act-type that exists on lex.bg. | **Confirmed:** ПМС (постановления на МС) — 0 in corpus (`grep -rli '^titulo:.*ПОСТАНОВЛЕНИЕ'` → 0); evidence = НСС (Национални счетоводни стандарти) = ПМС № 46/2005 (lex.bg ldoc 2135501600, **active**) is entirely absent. **Likely also omitted:** тарифи, инструкции (lex.bg search exposes a `search_for_instr` flag), решения на МС, разпореждания, укази. This is the SET-level analogue of the SUBDIVISION-level D-047: an **allowlist** (the 5 crawled categories) silently drops everything outside it — exactly as `CLASS_MAP` dropped unmapped subdivisions. Orthogonal to the D-047 body re-bootstrap (D-047 fixes the *content* of acts we HAVE; D-049 adds the act-*sets* we LACK). **Owner (2026-07-01): "this is absurd and must be fixed", recorded as next-due → FR-025.** Fix outline in FR-025: enumerate ALL lex.bg categories → owner scope decision → add corpus dirs + discovery + index + `rango` values (protected surfaces → preflight) → a **corpus-level coverage gate** (analogue of the per-act gate) → fetch via the CF-hardened pipeline (D-047 Task 9). **DISCOVERY FINDING (2026-07-02, `docs/research/2026-07-02-fr025-category-gap.md`): the original fix outline is INVALID.** lex.bg exposes ONLY 5 browsable act-type trees (`laws`/`code`/`ords`/`regs`/`reg_laws`); ПМС/тарифи/инструкции/решения/укази are NOT tree-browsable at all (proven: every candidate `tree/{slug}/0` returns the generic homepage with 1 ldoc link vs `laws/0`'s 36; the known active ПМС ldoc 2135501600 links only the 5 trees; site search is keyword-only, not an act-type enumeration). So the cause is a **source-capability limit**, not a crawl allowlist — there are no extra trees to add. **Owner decisions (2026-07-02):** (1) FR-025 sourcing MERGES into the FR-024 / roadmap-Phase-3 **ДВ (Държавен вестник) acquisition layer** — the only complete by-act-type source; (2) **interim = on-demand per-act capture** (fetch a specific ПМС/тарифа by `ldoc` when a consumer needs it, as done for the НСС decree) — the corpus documents this as a known coverage gap, not silent completeness; (3) the planned COMPREHENSIVE solution is a **top-down delegation-chain discovery** (to be brainstormed): every lesser act is issued under an explicit delegation in a higher act (наредба ← закон „издава се на основание чл. X…"), so mine the delegation references in acts we already hold (закони, наредби) to DERIVE the set of lesser acts that should exist, then locate each via keyword search + candidate authoritative sources (APIS `web-api.apis.bg`, MoJ/Ciela `justice.government.bg`), version-aware. This makes completeness provable against a DERIVED expected-set (D-047 discipline) rather than a source enumeration. FR-025 tree-crawl plan (`docs/plans/2026-07-02-fr025-category-coverage-plan.md`) Phases 3/5 SUPERSEDED; re-brainstorm pending. | Active (redirected) |
| D-050 | 2026-07-02 | **Pre-UI hardening track opened; sourcing tracks (FR-025/FR-022) parked behind it (owner).** A comprehensive software-stack code review (4 parallel module reviewers + orchestrator adversarial verification — `docs/research/2026-07-02-pre-ui-code-review.md`) found **6 verified P0s** (error taxonomy flattened on the wire; alinea parser corrupting 116 live paragraph rows; non-atomic full rebuild; coverage-gate middle-truncation blind spot; CF-challenge-on-200 + no titulo precondition; estado un-repeal on re-scrape) **+ a deterministic search-perf regression** (FR-027). Owner approved fixing ALL before building the UI / daily MCP use. Plan: `docs/plans/2026-07-02-pre-ui-hardening-plan.md` (18 tasks; Batches A index-correctness / B wire-contract / E acquisition-gates / C perf / D operability). | Owner scope decisions ratified (2026-07-02): (1) the UI = `legalize-bg-web` Next.js (sister repo, docs-only scaffold) consuming a **FastAPI REST API in THIS repo** per the approved Phase-7 design — registered as **FR-028**; its plan follows once hardening Batch B lands; the MCP server stays the agent-facing stdio surface. (2) Search perf re-engineered NOW (FR-027, measurement-gated; budget re-ratification = **D-051** at implementation — the ACTIVE.md "load-flaky" label was WRONG, failures are deterministic post-D-047: 223M FTS chars, cold search p95 3.0s vs 0.25s budget, "лични данни" 4.9s warm). (3) Acquisition-gate P0s ride in this plan; remaining acquisition P1s (bootstrap resume/idempotent-commit, dirty-tree preflight, history-shrinkage detection, bare-text `_walk`) attach to the FR-025 kickoff preflight. (4) MCP global lock stays for stdio — **FR-029** records the per-call-connection trigger; the REST API owns per-request connections and does not inherit the lock. Batch-B contract deltas (JSON error wire format, INVALID_DATE code, real output schemas, tools.json 1.3.0) are additive per Surface 3 — preflight `IMPLEMENTATION-PREFLIGHT-2026-07-02-wire-contract.md` (written at execution). **Outcome (2026-07-02):** all 5 batches (A/B/E/C/D, 18 tasks) landed on `main` through `2a3d5767`, CI green — 6/6 P0s fixed, `tools.json` 1.3.0, FR-027 search-perf regression resolved (see **D-051** for the perf decision + re-baselined budgets). Close-out gates: 471 non-perf + 7/7 perf tests green, `export_tools --check` OK, live 7-tool client smoke OK, CI green on the final push. Plan executed to completion; disposition recorded in `docs/research/2026-07-02-pre-ui-code-review.md`. | Active |
| D-051 | 2026-07-02 | **FR-027 search-perf fix ratified: option (a) title-first tier-2 gating + option (c) re-baseline; option (b) (split body FTS index) deferred.** `index/fts.py:search_fts`'s tier-1/tier-2 early-return widened from `len(title_rows) >= limit` to `len(title_rows) >= min(limit, 3)` — tier 2 (the full-corpus body `MATCH`, measured at 97.9-99.9% of query latency, `docs/research/2026-07-02-fr027-search-perf.md` Experiment C) now runs only when the title tier can't serve the query alone. All locked ranking tests (`tests/index/test_fts.py`, `test_fts_regression.py`, FR-015 adversarial fixture) passed with zero edits. Post-gating probe (`scripts/perf_probe.py`, 2 runs): 8/10 representative queries now title-served and fast (e.g. "лични данни" — one of the two pathological baseline queries — dropped to ~35ms cold / ~1ms warm); "административни нарушения" remains genuinely body-only (title tier < 3 hits) and stays 5.4-6.5s, matching the pre-gating baseline exactly — the documented, accepted "body-only queries stay slow" case option (c) covers rather than fixes structurally. | **Ratified budgets** (`tests/perf`, `perf` pytest marker added, excluded from CI): `search_p95` 0.100s→**0.020s**, `search_cold_p95` 0.250s→**0.050s** (rule: measured p95 × 1.5, widened further after re-run testing showed the literal ×1.5-of-clean-measurement value (~0.003s/0.007s) intermittently failed — traced via `ps aux` to this machine running 4 concurrent Claude Code sessions + browser/VM load at measurement time, not a code regression; 20/20 clean re-runs at the widened values). `get_law_*`/`get_article_*` budgets unchanged (SQL-only, outside D-051 scope). **New test** `tests/perf/test_warm_persistent.py::test_search_warm_persistent_p95` locks the persistent-connection-with-pragmas path (Task 13's win, `mcp_server/__main__.py:main()`'s actual model — previously untested) at **0.036s** (Task 13 measured 17-24ms × 1.5); 15/15 clean. Full non-perf suite 471/471 green (`-m "not perf"`); full perf suite 7/7 green. Deferred (b) trigger: web PRD 300ms p95 missed for the real query mix once the REST API (FR-028) ships. Research doc Decision section: `docs/research/2026-07-02-fr027-search-perf.md`. | Active |
| D-052 | 2026-07-02 | **REST API v1 shipped (FR-028), closing the plan opened under D-050 scope.** `api/app.py:create_app(db_path, corpus_root, cors_origins)` wires 7 endpoints (`/api/v1/laws`, `/laws/{slug}`, `/laws/{slug}/articles/{art}`, `/laws/{slug}/history`, `/laws/{slug}/diff`, `/search`, `/stats`) + `/healthz` + `/api/v1/metrics` over the shared query layer (`mcp_server/queries.py`, `index/fts.py`). Error taxonomy → HTTP mapping (`api/errors.py:HTTP_STATUS_BY_CODE`): INVALID_DATE/INVALID_ARTICLE_SPEC/INVALID_DATE_RANGE/QUERY_TOO_BROAD→400, LAW_NOT_FOUND/ARTICLE_NOT_FOUND/NO_VERSION_AT_DATE→404, AMBIGUOUS_NAME→409, DIFF_FAILED→500, INDEX_MISSING/INDEX_STALE→503 (unmapped codes fall back to 500); JSON body is `ToolError.to_dict()`, byte-compatible with the MCP wire format (D-050 Batch B). Response shapes reuse `mcp_server/schemas.py` TypedDicts (`GetLawResponseDict`, `GetArticleResponseDict`, `VersionEntryDict`, `SearchHitDict`) plus new REST-only TypedDicts in `api/schemas.py` (`LawListResponseDict`, `StatsResponseDict`, `DiffResponseDict`) for shapes with no MCP analogue. | Each request opens its own per-request `mode=ro` SQLite connection with the D-051 read pragmas (`api/deps.py:get_conn`) instead of reusing the MCP server's process-wide `_db_lock` (D-040/D-023) — the two transports' concurrency models are independent, so **FR-029 is unaffected** and stays its own backlog item on its own trigger. OpenAPI contract locked at `docs/api/openapi-rest.json` (generated via `api.export_openapi`, `--check`-verified in CI, `tests/api/test_export_openapi.py`) — the REST analogue of `tools.json`/`export_tools --check`. Live-smoke-verified against the real 3,601-act catalog (2026-07-02): `stats.total_acts == 3601`, all 9 routes returned valid JSON matching their locked TypedDict shapes. Plan `docs/plans/2026-07-02-fr028-rest-api-plan.md` (9 tasks) executed to completion; closes FR-028. Next: Phase 7.2 frontend (`legalize-bg-web` repo). | Active |
